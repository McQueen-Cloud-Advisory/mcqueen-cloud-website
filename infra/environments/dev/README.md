# Development Environment

This directory will contain the root Terraform configuration for development infrastructure.

Development resources should remain cost-controlled and should be created only when they support an actual application or validation requirement.
