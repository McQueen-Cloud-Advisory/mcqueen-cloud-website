# Production Environment

This directory will contain the root Terraform configuration for production supporting infrastructure.

Production changes will require a reviewed Terraform plan and an intentional apply step.
